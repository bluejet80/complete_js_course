/* 
const firstName = "Jarett"
const job = "Manager"
const birthYear = 1980;
const year = 2037

const jarett = "I'm " + firstName + ', a ' + (year - birthYear) + ' year old ' + job + '!';
console.log(jarett);

const jarettNew = `I'm ${firstName}, a ${year - birthYear} year old ${job}!`
console.log(jarettNew);

*/
/*
const age = 16;
const isOldEnough = age >= 18;

if (isOldEnough) {
    console.log(`Sarah can start drivers License.`)
} else {
    const yearsLeft = 18 - age;
    console.log(`Sarah is not old enough.
    Wait another ${yearsLeft} years.`)
}

const birthYear = 1980;

let century;
if (birthYear <= 2000) {
    century = '20th';
} else {
    century = '21st';
}

console.log(`You were born in the ${century} century.`)
*/
/*
const hasDriversLicense = true; // A
const hasGoodVision = true; // B

console.log(hasDriversLicense && hasGoodVision);
console.log(hasDriversLicense || hasGoodVision);


// if (hasDriversLicense && hasGoodVision) {
//     console.log(`Sarah is able to drive.`)
// } else {
//     console.log(`Someone else should drive...`)
// }

const isTired = false; // c


if (hasDriversLicense && hasGoodVision && !isTired) {
    console.log(`Sarah is able to drive.`)
} else {
    console.log(`Someone else should drive...`)
}

*/
/*
const day = 'hamburger';

switch (day) {
    case 'monday':
        console.log(`Plan my course structure`);
        console.log(`Go to coding meetup.`);
        break;
    case 'tuesday':
        console.log(`Eat a Sandwhich`);
        break;
    case 'wednesday':
    case 'thursday':
        console.log(`Write code examples`);
        break;
    case 'friday':
        console.log(`Record Videos`);
        break;
    case 'saturday':
    case 'sunday':
        console.log(`Enjoy the weekend :D`);
        break;
    default:
        console.log(`Not a valid Day of the week.`);
}
*/

